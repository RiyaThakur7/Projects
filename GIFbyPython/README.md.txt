# GIF by Python

This Python project creates a GIF from two images.

## Files
- `team-pic1.png` and `team-pic2.png` : input images
- `create_gif.py` : Python script that generates the GIF

## How to run
1. Make sure both images are in the same folder as `create_gif.py`.
2. Install required package if not installed: